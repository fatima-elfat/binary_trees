#include "binary_trees.h"
/**
 * binary_tree_is_heap -  checks if a binary
 * tree is Max Binary Heap.
 * @tree: a pointer to the root node of the tree to check.
 * Return: 1 on success.
*/
int binary_tree_is_heap(const binary_tree_t *tree)
{
		
}
